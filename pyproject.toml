[project]
name = "mcp-toolhub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "asxvgxkep", email = "2212448547@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.0.0",
]

[project.scripts]
mcp-toolhub = "mcp_toolhub:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
